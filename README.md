#Super simplified version of using web sockets

This just passes messages from connected clients to each other.

To run:
npm install ws

Then start the server:
node server.js

Then open the index.html file.
